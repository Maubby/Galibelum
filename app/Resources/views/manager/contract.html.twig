{# app/Resources/views/manager/index.html.twig #}
{% extends 'base_back.html.twig' %}

{% block title %}Contractualisation{% endblock %}

{% block stylesheets %}

    <!-- Additional CSS links-->
    <link rel="stylesheet" href="{{ asset('build/organization_show.css') }}">
    <link rel="stylesheet" href="{{ asset('build/admin.css') }}">
{% endblock %}

{% block header %}
    <!-- PASSED W3C VALIDATOR https://validator.w3.org/ -->
    <!-- Zero intern console warning -->

    <!-- Navbar in views/base_back.html.twig -->
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row boxing-canvas mt-30 mb-50">
            <div class="col-md-12 col-sm-12 col-xs-12 text-center">
                <i class="fas fa-briefcase text-center degradeblue" style="font-size: 50px"></i>
                <h5 class="degradeblue text-center">Contractualisation</h5>

                <!-- Creating a responsive table -->
                <div class="table-responsive">
                    <table id="table-manager" class="table table-striped text-center">
                        <!-- Table head -->
                        <thead>
                        <tr>
                            <th>Nom de la structure</th>
                            <th>Nom de l' activité</th>
                            <th>Nom de l'offre</th>
                            <th>Montant demandé</th>
                            <th>Avant le</th>
                            <th>Nom du partenaire</th>
                            <th>Statut</th>
                            <th>Mail à envoyer</th>
                        </tr>
                        </thead>
                        <!-- End of table head -->

                        <!-- Starting listing the offers -->
                        <tbody>
                        {% for organization in organizations %}
                            {% for contract in organization.contracts %}
                                <tr>
                                    <td>{{ contract.activity.organizationActivities.name }}</td>
                                    <td>{{ contract.activity.name }}</td>
                                    <td>{{ contract.name }}</td>
                                    <td>{{ contract.amount }}</td>
                                    <td>{{ contract.date|date('d M Y') }}</td>
                                    <td>{{ organization.name }}</td>

                                    <!-- Buttons changing contracts' status start -->
                                    <td>
                                        {% if contract.status == 0 %}
                                            <a href="{{ path('manager_contract_status', { 'id': contract.id, 'status': 1 }) }}"
                                               class="button button text-center">Aucun partenaire</a>
                                        {% elseif contract.status == 1 %}
                                            <a href="{{ path('manager_contract_status', { 'id': contract.id, 'status': 2 }) }}"
                                               class="button button-yellow text-center">Négociation</a>
                                        {% elseif contract.status == 2 %}
                                            <a href="{{ path('manager_contract_status', { 'id': contract.id, 'status': 3 }) }}"
                                               class="button button-green text-center">Validation</a>
                                        {% elseif contract.status == 3 %}
                                            <a href="{{ path('manager_contract_status', { 'id': contract.id, 'status': 4 }) }}"
                                               class="button button-blue text-center">Paiement</a>
                                        {% elseif contract.status == 4 %}
                                            <a href="{{ path('manager_contract_status', { 'id': contract.id, 'status': 5 }) }}"
                                               class="button button-purple text-center">Expiré</a>
                                        {% elseif contract.status == 5 %}
                                            <a href="{{ path('manager_contract_status', { 'id': contract.id, 'status': 0 }) }}"
                                               class="button button-red text-center">Refusé</a>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if contract.status == 2 %}
                                            <a href="{{ path('manager_contract_status_mail', { 'id': contract.id, 'status': 2 }) }}"
                                               class="button button-green text-center">Envoyer</a>
                                        {% elseif contract.status == 3 %}
                                            <a href="{{ path('manager_contract_status_mail', { 'id': contract.id, 'status': 3 }) }}"
                                               class="button button-blue text-center">Envoyer</a>
                                        {% elseif contract.status == 4 %}
                                            <a href="{{ path('manager_contract_status_mail', { 'id': contract.id, 'status': 4 }) }}"
                                               class="button button-purple text-center">Envoyer</a>
                                        {% elseif contract.status == 5 %}
                                            <a href="{{ path('manager_contract_status_mail', { 'id': contract.id, 'status': 5 }) }}"
                                               class="button button-red text-center">Envoyer</a>
                                        {% endif %}
                                    </td>
                                    <!-- Ending buttons changing contracts' status -->
                                </tr>
                            {% endfor %}
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block footer %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('build/menu_toggle.js') }}"></script>
    <script src="{{ asset('build/table_manager.js') }}"></script>
{% endblock %}